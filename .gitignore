# Dependencies (any level)
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz
.pnpm-store/

# Builds / output (any level)
dist/
build/
out/
.next/
coverage/
*.tsbuildinfo
next-env.d.ts

# Env / secrets — never commit (keep .env.example)
.env
.env.*
!.env.example

# Clerk / Vercel
.clerk/
.vercel

# Legacy MVP runtime (source kept; build/runtime ignored)
legacy/data/
legacy/outputs/
legacy/public/uploads/

# Vendored skill with its own .git
.claude/skills/huashu-design/

# OS / editor / logs
.DS_Store
*.pem
*.log
npm-debug.log*
.vscode/
